# 白读 · ByRead 依赖清单
# 安装：python -m pip install -r requirements.txt
#
# 说明：不锁死到旧版本。实测在 Python 3.14 上 lxml 6.x 已提供二进制 wheel，
# 无需本地编译工具链。

Flask>=3.0
requests>=2.31
feedparser>=6.0.11
lxml>=5.0
readability-lxml>=0.8.1
